import { MarkdownStrongPipe } from './markdown-strong.pipe';

describe('MarkdownStrongPipe', () => {
  it('create an instance', () => {
    const pipe = new MarkdownStrongPipe();
    expect(pipe).toBeTruthy();
  });
});
